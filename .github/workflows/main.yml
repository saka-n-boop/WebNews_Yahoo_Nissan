name: Yahoo News Scraper and Gemini Analyzer

on:
  workflow_dispatch:
  # 定期実行 (UTC 05:55 = JST 14:55)
  schedule:
    - cron: '25 05 * * *'

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          
          # 【重要】プログラムに合わせて最新のSDKを指定 (google-genai)
          pip install --no-cache-dir --upgrade google-genai

      - name: Run Python script
        env:
          GCP_SERVICE_ACCOUNT_KEY: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}
          GOOGLE_API_KEY_1: ${{ secrets.GOOGLE_API_KEY_1 }}
          GOOGLE_API_KEY_2: ${{ secrets.GOOGLE_API_KEY_2 }}
          GOOGLE_API_KEY_3: ${{ secrets.GOOGLE_API_KEY_3 }}
          SPREADSHEET_KEY: ${{ secrets.SPREADSHEET_KEY }}
        run: python main.py
